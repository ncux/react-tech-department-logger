import React from "react";
import { BrowserRouter, Route, Switch, Redirect } from "react-router-dom";

export const Router = () => {

    return (
        <BrowserRouter>
            <Switch>

            </Switch>
        </BrowserRouter>
    );

};
